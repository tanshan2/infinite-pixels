# WebGL 高细节体素地球设计验收

## 对照目标

- 视觉真值：`assets/earth-model-v2.png`
  - 像素尺寸：1254 × 1254
  - 内容：用户选定的高细节三维体素地球
- 页面实现：`implementation-step-fix-default-674x912.png`
  - 内置浏览器 CSS 视口与截图像素：674 × 912
  - 设备像素比：1
- 同屏比较：`qa-webgl-comparison.png`
  - 参考图与实现地球在同一浏览器画面内归一化为相近直径后比较
- 状态：中文、默认视角、未激活；地球位于传送门后方

参考图只定义地球本体，不定义完整网页构图。传送门、地点标签和中文界面沿用此前已确认的网站设计。

## 全景与聚焦证据

- `implementation-webgl-voxel-674x912.png` 显示地球完整位于传送门后方，传送门没有被新渲染器遮挡。
- `qa-webgl-comparison.png` 同时显示参考图和实现地球，聚焦比较球面轮廓、格子密度、块体厚度、海岸线、颜色和光照；无需额外局部截图，因为归一化后的每个体素在同屏图中可辨认。
- 当前 674 像素宽视口采用 256 × 128 实例；宽度不超过 480 像素时采用 160 × 80。地理遮罩为 720 × 360。

## 五项保真检查

### 字体与排版

参考图没有网页文字。实现保留既有中文地点名称、语言按钮和传送门牌匾，没有引入普通英文；文字位于地球覆盖层，不影响体素本体清晰度。

### 间距与布局节奏

地球尺寸、垂直位置和传送门前后关系保持原设计。球体轮廓完整，下缘被传送门自然遮挡；没有因 WebGL Canvas 内部分辨率变化导致布局跳动。

### 颜色与视觉变量

海洋为深青绿色；陆地以橄榄绿为基础，左上进入暖金色高光，右侧和下方压暗。最终同屏比较中，主色关系、光源方向和边缘暗化与参考图一致。

### 图片质量与资源保真

地球由真实 WebGL2 倒角块体实时绘制，不使用参考图作为页面贴图。每个实例具有顶面、倒角面和侧面，深度缓冲负责遮挡；Canvas 按设备像素比自适应。传送门继续使用既有透明 PNG 成品资产，没有重绘或替换。

### 文案与内容

默认中文文案保持“中文 · 英文”“全部作品”“鼠标双击地球唤醒”等既有内容。作品入口与地点经历的信息架构没有改变。

## 交互与技术验收

- 初始状态：`globe3d-stage` 未包含 `is-active`，控制按钮带 `hidden`。
- 双击激活：类名变为 `globe3d-stage is-active`，无障碍说明变为“已唤醒的像素地球，可拖动旋转”，控制按钮显示。
- 拖拽旋转：拖拽前后浏览器截图字节长度与采样值均改变，证明 WebGL 画面随手势更新。
- 缩放：点击放大后 Canvas 样式变为 `transform: scale(1.16364)`。
- 传送门层级：主 Canvas 为 `z-index: 1`，地点覆盖层为 `z-index: 2`，传送门为 `z-index: 4`。
- 浏览器日志：最终默认页与交互页均为空数组，无错误或警告。
- 自动化测试：24 项测试覆盖状态机、中文文案、页面结构、地理拓扑、体素网格、倒角几何、实例数据和 WebGL 接线。

## 比较历史

### 第一轮：二维 Canvas 方案

- [P1] 体素只有平面顶面，缺少真实倒角和侧面。
- [P1] 88 × 44 密度明显低于参考图。
- [P2] 默认镜头与光照层次偏离参考图。
- 修复：更换为原生 WebGL2 实例化倒角体素，并设置大西洋默认视角。

### 第二轮：首个 WebGL 版本

- [P2] 176 × 88 在实际页面显示中仍偏粗，海岸细节低于参考图。
- [P2] 陆地正面过黄且高度变化不足。
- 修复：宽屏密度提高到 256 × 128，手机密度提高到 160 × 80；遮罩提高到 720 × 360；增大倒角、陆地高度差和稳定逐格变化；重新标定暖光与橄榄绿基础色。

### 第三轮：最终同屏复核

- 证据：`qa-webgl-comparison.png`。
- 先前 P1/P2 已消除：实现具有可辨认的独立立方块、连续无错误孔洞的大陆、与参考接近的体素密度、左上暖光和右侧深绿阴影。
- [P3] 参考图是风格化生成地图，部分岛屿和逐格高度具有艺术随机性；实现使用真实 GeoJSON，因此个别海岸小块不会逐格完全相同。这是数据真实性带来的可接受差异，不影响选定视觉方向。

### 第四轮：斜视阶梯修正

- 用户反馈证据：`codex-clipboard-3b627cb5-14da-49f8-967b-2616baeb7817.png` 显示亚洲斜视角出现连续平行阶梯。
- 根因：陆地块高约为单格高度的 2–4 倍；旧的线性取模变化还会形成规则斜向条纹；球面切线方向在斜视角下进一步放大侧面层次。
- 修复：陆地块高降至约一个体素（0.024），变化幅度降至 0.05，改用稳定二维散列；海洋块高降至 0.011。传送门、视角交互、密度和布局未改动。
- 修复后证据：`implementation-step-fix-default-674x912.png` 与 `qa-webgl-comparison.png`；大陆表面保持体素质感，连续台阶被压回单层侧面。

## 最终结果

final result: passed
